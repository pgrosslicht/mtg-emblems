spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:~/db
    username: admin
    password:
  h2:
    console:
      settings:
        web-allow-others: true
      enabled: true
      path: /h2-console
logging:
  level.web: trace